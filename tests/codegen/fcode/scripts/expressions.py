# pylint: disable=missing-function-docstring, missing-module-docstring/
ai = 1
bi = ai
ci = ai + 1
di = ci * 2 + 3 * bi + ai
ti = ai + bi * (ci + di)

ad = 1.
bd = ad
cd = ad + 1.
dd = cd * 2. + 3. * bd + ad
td = ad + bd * (cd + dd)
