name: Benchmarks

on:
  push:
    branches: [ master, bench ]

jobs:

  Benchmark:

    runs-on: ubuntu-latest

    steps:
      - name: Set up ssh
        uses: webfactory/ssh-agent@v0.7.0
        with:
            ssh-private-key: ${{ secrets.SSH_DEPLOY_KEY }}
      - name: Clone pyccel-benchmarks repository
        working-directory: ../
        run: |
          git clone git@github.com:pyccel/pyccel-benchmarks.git
      - name: Push results to pyccel-benchmarks
        working-directory: ../pyccel-benchmarks
        run: |
          #git config --global user.email "yaman.guclu@gmail.com"
          #git config --global user.name "Yaman Güçlü"
          git commit --allow-empty -m "Benchmark of pyccel@${GITHUB_SHA}"
          git push
