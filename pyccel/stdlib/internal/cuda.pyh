# pyccel header for Cuda.

# ............................................................
#            Runtime Library Routines for C
# ............................................................

#$ header metavar ignore_at_import=True
#$ header metavar save=True

#$ header function array(*, *, str) results(*)
#$ header function deviceSynchronize()
#$ header function threadIdx(int)
#$ header function blockDim(int)
#$ header function blockIdx(int)
#$ header function gridDim(int)